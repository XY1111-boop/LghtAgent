以下是该文档的知识卡片：

**标题**：hashlib 与加密基础

**关键概念**：
- hashlib（Python哈希库）
- SHA-256（安全哈希算法）
- 对称加密（AES）
- 非对称加密（RSA）
- 密码安全与伦理

**摘要**：  
该文档介绍了Python中`hashlib`库的基本用法，通过`sha256()`对密码进行哈希处理并输出十六进制摘要。同时提及了两种主流加密方式：对称加密（如AES）和非对称加密（如RSA），并强调不应尝试破解他人密码，体现了密码使用的安全与伦理意识。