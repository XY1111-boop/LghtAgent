以下是根据您提供的文档内容生成的知识卡片：

**标题**：Redis 与 MongoDB 核心特性概述

**关键概念**：
- **Redis 命令**：`SET key value`（设置键值对）与 `GET key`（获取键对应的值）
- **MongoDB**：文档型数据库，以 JSON 类文档存储数据
- **应用场景**：缓存（Redis）、会话存储（Redis）

**摘要**：
本文档简要介绍了 Redis 的两个基本命令（SET 与 GET），并指出 MongoDB 是一种文档型数据库。同时说明了 Redis 适用于缓存和会话存储等场景。