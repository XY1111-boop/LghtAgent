以下是根据您提供的文档内容生成的知识卡片：

**标题**：JS基础核心概念

**关键概念**：
- `let` 和 `const` 声明变量
- 箭头函数语法
- DOM 操作与事件监听

**摘要**：
本文档介绍了 JavaScript 基础知识中的三个核心概念：使用 `let` 和 `const` 进行变量声明（`let` 用于可重新赋值的变量，`const` 用于常量）、箭头函数的简洁写法（`() => {}`），以及通过 DOM 操作获取元素并绑定事件（如 `document.getElementById('btn').addEventListener('click', () => alert('clicked'))`）。示例展示了如何为按钮添加点击事件并弹出提示框。