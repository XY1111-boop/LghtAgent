以下是为该文档生成的知识卡片：

---

**标题**：简易 Flask 应用与安全部署要点

**关键概念**：
- Flask 应用初始化：`Flask(__name__)`
- 路由定义：`@app.route('/')`
- 视图函数：`index()` 返回响应内容
- 安全部署：关闭 debug 模式、防止目录遍历

**摘要**：
该文档展示了一个最简单的 Flask Web 应用示例，通过 `Flask(__name__)` 创建应用实例，使用 `@app.route('/')` 定义根路径路由，视图函数返回 `'Hello World!'`。文档同时强调了部署到服务器时的关键安全配置：必须关闭 debug 模式（避免泄露错误信息）以及防止目录遍历攻击（限制静态文件访问路径），以确保应用在生产环境中的安全性。