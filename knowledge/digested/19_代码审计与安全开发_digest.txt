好的，以下是根据您提供的文档内容生成的知识卡片：

---

### 知识卡片：安全编码实践

**标题**：安全编码核心原则与工具

**关键概念**：
- **安全扫描工具**：`bandit`（Python代码安全分析）、`safety`（Python依赖库漏洞检查）
- **最小权限原则**：仅授予程序或用户完成其任务所必需的最小权限
- **输入校验**：对所有外部输入进行合法性验证，防止注入攻击
- **输出编码**：对输出到其他上下文（如HTML、SQL）的数据进行转义，防止跨站脚本（XSS）等攻击
- **职业操守**：构建安全软件是程序员的基本职业道德与责任

**摘要**：
本文档强调了安全编码的实践方法，包括使用 `bandit` 和 `safety` 等自动化工具扫描代码及依赖中的安全漏洞。同时，提出了三项核心安全原则：遵循最小权限以限制潜在损害，严格进行输入校验以防止恶意数据注入，以及实施输出编码以防御跨站脚本等攻击。最后，文档指出，构建安全的软件不仅是技术需求，更是程序员应具备的职业操守。